# PostgreSQL connection for persistent storage (recommended for Vercel)
# Example (local):
# POSTGRES_URL=postgresql://user:password@localhost:5432/ticket_booking
# or use DATABASE_URL with the same value.
#
# On Vercel, add POSTGRES_URL in Project Settings -> Environment Variables.
POSTGRES_URL=
DATABASE_URL=
